export interface ICode{
code: string | null 
}

export interface IAutorisationAxiosResp{

}

export interface IResponseBody {
    accessToken: string;
    refreshToken: string;
    expiresIn: number;
}